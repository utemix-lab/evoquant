# Architecture

- Layers: proposal / discourse / research / manuscript / automation / interfaces / docs
- Meta-project integration: registry.yml, crosslinks, adapters
- LLM swap: adapters/llm_adapter.yaml, embed_adapter.yaml; prompt_contract.md; fallback policy
- Operations: watchdog heartbeat, 5-min timebox, 1–2 files per push
